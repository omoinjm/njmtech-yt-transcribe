services:
  yt-transcribe:
    image: ${DOCKERHUB_USERNAME}/njmtech-yt-transcribe:latest
    container_name: yt-transcribe-app
    command: -db
    env_file: .env
    # Mount /tmp so downloaded audio files are written outside the container layer.
    volumes:
      - /tmp:/tmp
    restart: "no"

